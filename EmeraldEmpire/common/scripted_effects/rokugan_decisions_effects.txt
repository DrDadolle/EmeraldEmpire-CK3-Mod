reborn_snake_clan_scripted_effect = {
	create_title_and_vassal_change = {
		type = created
		save_scope_as = change
		add_claim_on_loss = no
	}
	title:d_chuda = {
		change_title_holder = {
			holder = root
			change = scope:change
		}
	}
	# Recreate de-jure lands
	title:c_chuda_province = {
		set_de_jure_liege_title = title:d_chuda
		set_coa = dynasty:2100115
	}

	give_nickname = nick_reborn_snake

	# every_held_title = {
	# 	# only county directly own by the character
	# 	limit = { tier = tier_county }
	# 	every_county_province = {
	# 		set_county_culture = culture:chuda
	# 	}
	# }



	# Change Dynasty to Chuda @Check AGOT
	# dynasty = {
	# 	save_scope_as = old_dynasty
	# }
	# set_dynasty = dynasty:2100115	
	
}